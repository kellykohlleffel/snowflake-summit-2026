# Dependencies
node_modules/
.venv/
__pycache__/

# Build outputs
dist/
*.vsix

# Credentials (never commit)
.env
!.env.example
# Per-laptop credential files — only *.env files in setup/creds are ignored,
# so README.md and .gitkeep remain tracked to preserve the directory.
setup/creds/*.env

# OS
.DS_Store

# Screenshots
screenshots/

# Firebase
.firebase/
